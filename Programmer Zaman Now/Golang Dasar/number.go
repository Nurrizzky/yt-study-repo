package main

import "fmt"

func main() {
	fmt.Println("satu : ", 1)
	fmt.Println("dua : ", 2)
	fmt.Println("tiga koma lima : ", 3.5)
}

// Tipe data ::
// NUMBER
// > INT = bilangan bulat dan Floating Point = bilangan desimal
// Ada 2 tipe int = INT dan UINT
// int8, int16, int32, int64
// Int = nilai minimun dari - ke maximum + == kalo butuh negatif pakai ini
// uint = nilai minimun dari 0 ke maximum + == kalo tidak butuh negatif memakai ini

// FLOATING POINT = Bilangan Desimal (ada coma(,))
// > float32 = nilai minimum = 1.18x10Pangkat-38 maximum = ====38
// > complex (Sebenarnya jarang digunanakan)

// ALIAS = Nama lain == tipe datanya sudah ada
// > byte == alias untuk uint8
// > rune == alias untuk int32
// > int == alias untuk minimal int32
// > uint == alias untuk minimal uint32